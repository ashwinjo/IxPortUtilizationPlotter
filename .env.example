# ========================================
# Docker Services Configuration
# ========================================

# Service Ports (customize if defaults conflict with existing services)
INFLUXDB_PORT=8087
PROMETHEUS_PORT=9090
GRAFANA_PORT=3005

# InfluxDB Configuration
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=Keysight12345!
INFLUXDB_ORG=keysight
INFLUXDB_BUCKET=ixosChassisStatistics
INFLUXDB_TOKEN='IgqaXL7maH-o3Hf8o0knuOQZAzF6CxC6xQ9OZVkH_lWaGyOiMk20Gnr82zIn0uvuyn67DdxTTDOPRLEZWKTAMw=='
INFLUXDB_RETENTION=0  # 0 = infinite retention, or specify in seconds

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ========================================
# Poller Configuration (Optional)
# ========================================
# Blocked ports API — used to classify port status (Free/Utilized/Blocked)
# BLOCKED_PORTS_URL=http://localhost:8890/api/v1/ports/blocked

# Chassis credentials source (priority 1 — overrides env var and hardcoded defaults)
# Points to IxiaInventoryExplorer API. Leave unset to use default.
# CREDENTIALS_URL=http://host.docker.internal:3001/api/config/credentials
# The poller runs on your HOST machine (not in Docker)
# These variables are read by config.py if set

# If not set, config.py uses its default values

# InfluxDB Connection URL for poller
# Use localhost if services run on same machine, or remote host IP
INFLUXDB_URL=http://localhost:8087


# Polling interval in seconds - This is for my influxDB to select metrics push intevals
POLLING_INTERVAL=30
# Polling interval in seconds - This is for my prometheus to select metrics push intevals
POLLING_INTERVAL_PERF_METRICS=30
POLLING_INTERVAL_SENSOR_METRICS=30
CHASSIS_LIST = [{"ip": "10.36.75.205", "username": "admin", "password": "admin"}]
